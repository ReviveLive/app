# Keep the build context small and the image clean. Paths are relative to the
# build context (the project root). Without this, Docker would upload your whole
# local node_modules and any old build output.
frontend/node_modules
frontend/dist
**/__pycache__
backend/.venv
pgdata
processed
incoming
.git
