# Copy to .env and adjust. Never commit .env.

# Postgres + TimescaleDB (local Docker)
POSTGRES_USER=revive
POSTGRES_PASSWORD=changeme
POSTGRES_DB=revive_pilot
DATABASE_URL=postgresql://revive:changeme@localhost:5432/revive_pilot

# Where the periodic O'Dwyer .xlsx file lands locally
INCOMING_DIR=./incoming
